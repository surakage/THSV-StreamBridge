// Creator Utility Pack provides small cross-platform counters, chat giveaways, and local polls.
// It is intended for community interaction, not regulated or high-value prize drawings.
const MODULE_ID = 'thsv.creator-utility-pack';
const manifest = { contractVersion: '2.0.0-preview.1', moduleId: MODULE_ID, name: 'Creator Utility Pack', version: '2.5.1', minimumCoreVersion: '2.0.0-preview.1', maximumTestedCoreVersion: '2.0.0-preview.1', minimumBridgeVersion: '2.5.1', maximumTestedBridgeVersion: '2.5.1', dependencies: [], requiredCapabilities: [], configurationSchema: 'schemas/config.json', eventSubscriptions: ['command.received', 'stream.offline'], commandsProvided: [{ id: 'creator-utility.counter', name: 'counter' }, { id: 'creator-utility.enter', name: 'enter' }, { id: 'creator-utility.giveaway', name: 'giveaway' }, { id: 'creator-utility.vote', name: 'vote' }, { id: 'creator-utility.poll', name: 'poll' }], actionsProvided: [], browserSourcesProvided: [], dataStorageOwned: ['data/addons/thsv.creator-utility-pack/', 'data/addons/.state/thsv.creator-utility-pack/'], installationSteps: ['Install and choose command names.', 'Create matching no-response commands in Command Sync.', 'Use moderator/broadcaster commands to manage counters, giveaways, and polls.'], uninstallationSteps: ['Uninstalling preserves bounded counters and the current poll definition, but not in-memory giveaway identities.'], migrations: [], healthChecks: [{ id: 'thsv.creator-utility-pack.runtime', description: 'Confirms bounded utility state and source-routed replies are available.' }] };
let entrants = new Map(); let operation = Promise.resolve();
function text(value, max = 300) { return [...(typeof value === 'string' ? value.replace(/[\u0000-\u001f\u007f]/gu, ' ').replace(/\s+/gu, ' ').trim() : '')].slice(0, max).join(''); }
function id(value) { const result = text(value, 40).toLowerCase().replace(/[^a-z0-9-]/gu, '-').replace(/^-+|-+$/gu, ''); return /^[a-z][a-z0-9-]{0,39}$/u.test(result) ? result : ''; }
function settingsFor(context) { const raw = context.settings || {}; return { enabled: raw.enabled !== false, counterCommand: id(raw.counterCommand) || 'counter', giveawayCommand: id(raw.giveawayCommand) || 'giveaway', enterCommand: id(raw.enterCommand) || 'enter', pollCommand: id(raw.pollCommand) || 'poll', voteCommand: id(raw.voteCommand) || 'vote', closeOnStreamEnd: raw.closeOnStreamEnd !== false, maximumCounters: Number.isInteger(raw.maximumCounters) ? Math.min(100, Math.max(1, raw.maximumCounters)) : 25, maximumEntrants: Number.isInteger(raw.maximumEntrants) ? Math.min(5000, Math.max(10, raw.maximumEntrants)) : 500, maximumPollOptions: Number.isInteger(raw.maximumPollOptions) ? Math.min(10, Math.max(2, raw.maximumPollOptions)) : 6 }; }
function randomSalt() { const bytes = new Uint8Array(24); crypto.getRandomValues(bytes); return [...bytes].map((item) => item.toString(16).padStart(2, '0')).join(''); }
function stateFor(raw, settings) { const value = raw && typeof raw === 'object' ? raw : {}; const counters = {}; if (value.counters && typeof value.counters === 'object') for (const [key, count] of Object.entries(value.counters).slice(0, settings.maximumCounters)) if (id(key) && Number.isSafeInteger(count)) counters[id(key)] = Math.min(1_000_000_000, Math.max(-1_000_000_000, count)); const poll = value.poll && typeof value.poll === 'object' && Array.isArray(value.poll.options) ? { open: value.poll.open === true, question: text(value.poll.question, 180), options: value.poll.options.map((item) => text(item, 80)).filter(Boolean).slice(0, settings.maximumPollOptions), votes: value.poll.votes && typeof value.poll.votes === 'object' ? Object.fromEntries(Object.entries(value.poll.votes).filter(([key, choice]) => /^[a-f0-9]{64}$/u.test(key) && Number.isSafeInteger(choice)).slice(0, 5000)) : {} } : { open: false, question: '', options: [], votes: {} }; return { accountSalt: /^[a-f0-9]{48}$/u.test(value.accountSalt) ? value.accountSalt : randomSalt(), counters, giveawayOpen: value.giveawayOpen === true, poll }; }
function moderator(event) { const roles = new Set((event.user?.roles || []).map((role) => text(role, 30).toLowerCase())); return roles.has('broadcaster') || roles.has('moderator') || roles.has('mod'); }
async function hashAccount(event, salt) { const raw = `${salt}|${event.platform}|${text(event.user?.id, 256)}`; const bytes = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(raw)); return [...new Uint8Array(bytes)].map((item) => item.toString(16).padStart(2, '0')).join(''); }
async function reply(context, event, message) { return context.chat.send({ message: text(message, 500), routing: 'source', sourcePlatform: event.platform, overflow: 'split' }); }
async function process(event, context) { const settings = settingsFor(context); if (!settings.enabled) return; const state = stateFor(await context.state.read(), settings); if (event.eventType === 'stream.offline' && settings.closeOnStreamEnd) { state.giveawayOpen = false; state.poll.open = false; entrants.clear(); await context.state.write(state); return; } if (event.eventType !== 'command.received' || event.metadata?.simulated === true || event.user?.actorType !== 'human') return;
  const command = id(event.payload?.command); const args = Array.isArray(event.payload?.arguments) ? event.payload.arguments.map((item) => text(item, 180)).filter(Boolean) : [];
  if (command === settings.counterCommand) { const name = id(args[0] || 'default'); if (!name) return reply(context, event, 'Usage: !counter name [show|+1|-1|reset]'); const action = (args[1] || 'show').toLowerCase(); if (action !== 'show' && !moderator(event)) return reply(context, event, 'Only a moderator or broadcaster may change counters.'); if (!Object.hasOwn(state.counters, name) && Object.keys(state.counters).length >= settings.maximumCounters) return reply(context, event, 'The counter limit has been reached.'); const current = state.counters[name] || 0; if (action === '+1') state.counters[name] = current + 1; else if (action === '-1') state.counters[name] = current - 1; else if (action === 'reset') state.counters[name] = 0; await context.state.write(state); return reply(context, event, `${name}: ${state.counters[name] || 0}`); }
  if (command === settings.giveawayCommand && moderator(event)) { const action = (args[0] || '').toLowerCase(); if (action === 'open') { entrants.clear(); state.giveawayOpen = true; await context.state.write(state); return reply(context, event, `Giveaway open! Use !${settings.enterCommand} to enter.`); } if (action === 'close') { state.giveawayOpen = false; await context.state.write(state); return reply(context, event, 'Giveaway entries are closed.'); } if (action === 'reset') { entrants.clear(); state.giveawayOpen = false; await context.state.write(state); return reply(context, event, 'Giveaway reset.'); } if (action === 'draw') { state.giveawayOpen = false; await context.state.write(state); const values = [...entrants.values()]; if (!values.length) return reply(context, event, 'No eligible giveaway entries were received.'); const winner = values[Math.floor(Math.random() * values.length)]; await context.overlay.publish(`${MODULE_ID}.result.show`, { title: 'Giveaway winner', text: winner, durationMs: 10000 }); return reply(context, event, `Giveaway winner: ${winner}!`); } return reply(context, event, `Use !${settings.giveawayCommand} open, close, draw, or reset.`); }
  if (command === settings.enterCommand) { if (!state.giveawayOpen) return reply(context, event, 'The giveaway is not open.'); const account = await hashAccount(event, state.accountSalt); if (!entrants.has(account) && entrants.size >= settings.maximumEntrants) return reply(context, event, 'The giveaway entry limit has been reached.'); entrants.set(account, text(event.user?.displayName || event.user?.name, 80) || 'Viewer'); return reply(context, event, 'You are entered.'); }
  if (command === settings.pollCommand && moderator(event)) { const action = (args[0] || '').toLowerCase(); if (action === 'open') { const parts = args.slice(1).join(' ').split('|').map((item) => text(item, 180)).filter(Boolean); if (parts.length < 3) return reply(context, event, `Usage: !${settings.pollCommand} open Question | Option 1 | Option 2`); state.poll = { open: true, question: parts[0], options: parts.slice(1, settings.maximumPollOptions + 1), votes: {} }; await context.state.write(state); return reply(context, event, `${state.poll.question} ${state.poll.options.map((item, index) => `${index + 1}) ${item}`).join(' ')}`); } if (action === 'close') { state.poll.open = false; await context.state.write(state); const totals = state.poll.options.map((_item, index) => Object.values(state.poll.votes).filter((choice) => choice === index).length); const result = state.poll.options.map((item, index) => `${item}: ${totals[index]}`).join(' • '); await context.overlay.publish(`${MODULE_ID}.result.show`, { title: state.poll.question || 'Poll results', text: result, durationMs: 12000 }); return reply(context, event, result || 'No poll is configured.'); } if (action === 'reset') { state.poll = { open: false, question: '', options: [], votes: {} }; await context.state.write(state); return reply(context, event, 'Poll reset.'); } }
  if (command === settings.voteCommand) { if (!state.poll.open) return reply(context, event, 'There is no open poll.'); const choice = Number(args[0]) - 1; if (!Number.isInteger(choice) || choice < 0 || choice >= state.poll.options.length) return reply(context, event, `Vote with !${settings.voteCommand} 1-${state.poll.options.length}.`); state.poll.votes[await hashAccount(event, state.accountSalt)] = choice; await context.state.write(state); return reply(context, event, `Vote recorded for ${state.poll.options[choice]}.`); }
}
export default { manifest, required: false, async start(context) { entrants.clear(); const settings = settingsFor(context); await context.state.write(stateFor(await context.state.read(), settings)); }, async stop() { await operation.catch(() => undefined); entrants.clear(); operation = Promise.resolve(); }, async onEvent(event, context) { operation = operation.then(() => process(event, context), () => process(event, context)); await operation; } };
export { stateFor };
