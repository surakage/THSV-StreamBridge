# Accessibility Captions

Publishes short-lived, high-contrast captions to `/overlay/addons/thsv.accessibility-captions`. Public chat is opt-in, bot/system messages are suppressed by default, text is bounded, and no caption bodies are stored. Use **Voice Relay** separately for filtered TTS; this add-on does not speak or retain messages.
