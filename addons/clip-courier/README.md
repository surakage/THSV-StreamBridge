# Clip Courier

Clip Courier performs bounded Twitch clip discovery and posts each stable clip ID once to either a Discord normal channel or forum. One private webhook belongs to one selected destination type. The first scan is a silent baseline by default. The Discord webhook stays only in the imported Streamer.bot Deliver action.

The add-on does not use third-party scanners, arbitrary JSON paths, OAuth tokens, or another WebSocket connection.
