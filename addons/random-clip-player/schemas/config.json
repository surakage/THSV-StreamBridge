{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Random Clip Player",
  "type": "object",
  "additionalProperties": false,
  "required": [],
  "properties": {
    "secondsBetweenClips": {
      "type": "integer",
      "title": "Pause between clips (seconds)",
      "description": "The complete pause from one clip ending until StreamBridge requests the next clip. The short fade happens during this pause; no hidden seconds are added.",
      "minimum": 1,
      "maximum": 3600,
      "default": 5
    },
    "clipCount": {
      "type": "integer",
      "title": "Clips to fetch per refresh",
      "description": "How many of your most recent Twitch clips to pull into the rotation pool at once.",
      "minimum": 1,
      "maximum": 100,
      "default": 20
    },
    "minDurationSeconds": {
      "type": "integer",
      "title": "Minimum clip length (seconds)",
      "minimum": 1,
      "maximum": 300,
      "default": 5
    },
    "maxDurationSeconds": {
      "type": "integer",
      "title": "Maximum clip length (seconds)",
      "minimum": 1,
      "maximum": 300,
      "default": 60
    },
    "muted": {
      "type": "boolean",
      "title": "Mute clip audio",
      "description": "Disable clip audio. Leave this off to play the clip's original sound in OBS, Meld, or Streamlabs.",
      "default": false
    },
    "volume": {
      "type": "number",
      "title": "Clip volume",
      "description": "Playback volume from 0 (silent) through 1 (full volume).",
      "minimum": 0,
      "maximum": 1,
      "default": 1
    },
    "cacheVideo": {
      "type": "boolean",
      "title": "Prefetch clip video locally",
      "description": "Optional. Downloads the selected Twitch clip before playback to reduce CDN stalls. Cached video expires automatically and is never shared outside the loopback overlay server.",
      "default": false
    },
    "cacheTtlHours": {
      "type": "integer",
      "title": "Cache lifetime (hours)",
      "minimum": 1,
      "maximum": 24,
      "default": 12
    },
    "cacheMaximumFileMb": {
      "type": "integer",
      "title": "Maximum cached clip size (MB)",
      "minimum": 5,
      "maximum": 50,
      "default": 40
    }
  }
}
