{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Village Polls — Universal Cross-Platform Polls",
  "type": "object",
  "additionalProperties": false,
  "x-distinctFields": ["pollCommand", "voteCommand"],
  "properties": {
    "enabled": { "type": "boolean", "title": "Turn on universal cross-platform polls", "description": "Uses the existing main platform intake actions and announces one shared poll to Twitch, YouTube, Kick, and TikTok. No separate poll trigger is needed.", "default": false },
    "commandPrefix": { "type": "string", "title": "Command prefix", "description": "One visible character placed before poll and vote, such as ! or ?.", "minLength": 1, "maxLength": 1, "pattern": "^\\S$", "default": "!" },
    "pollCommand": { "type": "string", "title": "Poll management command name", "description": "Enter the word without !. Moderators and the broadcaster use this command.", "pattern": "^[a-z][a-z0-9-]{0,39}$", "default": "poll" },
    "voteCommand": { "type": "string", "title": "Viewer vote command name", "description": "Enter the word without !. The default lets viewers vote with !vote 1.", "pattern": "^[a-z][a-z0-9-]{0,39}$", "default": "vote" },
    "closePollOnStreamEnd": { "type": "boolean", "title": "Close the poll when every platform is offline", "description": "Prevents an old poll from remaining open for the next stream.", "default": true },
    "maximumPollOptions": { "type": "integer", "title": "Choices allowed in one poll", "description": "Six is recommended so the compact poll remains readable.", "minimum": 2, "maximum": 10, "default": 6 },
    "pollDurationSeconds": { "type": "integer", "title": "Seconds before the poll closes", "description": "Use 0 to close polls manually. The default is two minutes.", "minimum": 0, "maximum": 3600, "default": 120 },
    "resultSeconds": { "type": "integer", "title": "Winner display time", "description": "How long the completed result stays visible.", "minimum": 5, "maximum": 60, "default": 12 },
    "layout": { "type": "string", "title": "Poll size", "description": "Compact uses the least screen space. Full gives long choices more room without becoming full-screen.", "enum": ["compact", "full"], "default": "compact" },
    "showPercentages": { "type": "boolean", "title": "Show percentages", "default": true },
    "showVoteCounts": { "type": "boolean", "title": "Show vote totals", "default": true },
    "showTimer": { "type": "boolean", "title": "Show countdown", "default": true },
    "showPlatformBreakdown": { "type": "boolean", "title": "Show platform vote markers", "description": "Adds a small Twitch, YouTube, Kick, and TikTok breakdown under choices that received votes.", "default": false },
    "transition": { "type": "string", "title": "Entrance and exit", "enum": ["slide", "fade", "pop"], "default": "slide" },
    "backgroundColor": { "type": "string", "title": "Glass background color", "pattern": "^#[0-9A-Fa-f]{6}$", "default": "#111923" },
    "backgroundOpacity": { "type": "number", "title": "Background opacity", "description": "Lower values show more of the stream behind the poll.", "minimum": 0.2, "maximum": 0.95, "default": 0.72 },
    "accentColor": { "type": "string", "title": "Accent and result bars", "pattern": "^#[0-9A-Fa-f]{6}$", "default": "#7ff5cc" },
    "textColor": { "type": "string", "title": "Text color", "pattern": "^#[0-9A-Fa-f]{6}$", "default": "#ffffff" }
  }
}
