# Bloom Companion archive

Historical Bloom engine, storage, browser assets, animation prototype, Streamer.bot package, scripts, documentation, and tests. The artwork and source are preserved for a future optional module.

Core no longer serves `/overlay/companion` or loads `data/state/companion.json`. Do not import the archived package into the Stage 2 preview.
