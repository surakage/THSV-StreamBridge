# Viewer Progression archive

Historical Viewer Identity and Progression source, package artifacts, administration script, documentation, and tests. Legacy receiver and Multi-Commands imports that exposed viewer identity are retained under `packages/legacy-core-contracts` for recovery only.

Do not import these packages into the Stage 2 preview. State in `data/state/viewer-progression.json` remains creator-owned and preserved but is not loaded by core.
