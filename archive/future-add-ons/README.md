# Archived future add-ons

This directory contains inert historical implementations extracted from StreamBridge core during Stage 2. Nothing here is discovered, loaded, built into the core release, or supported as an installable v2 module.

Each candidate must be migrated to public v2 contracts, given a valid module manifest and isolated storage ownership, and pass installation, removal, migration, and failure-isolation acceptance before reuse.
