// Product/release version shown to creators and compared with GitHub Releases.
// Keep protocol compatibility in bridge/contracts/v2/common.ts; update this value
// through `npm run release:sync-version -- <major.minor.patch>`.
export const STREAMBRIDGE_VERSION = '2.4.0' as const;
