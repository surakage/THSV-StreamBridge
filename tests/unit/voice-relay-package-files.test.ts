import { readFile } from 'node:fs/promises'; import { describe, expect, it } from 'vitest';
describe('Voice Relay package', () => { it('uses the documented TtsSpeak method and one dedicated group', async () => { const manifest = JSON.parse(await readFile('packages/streamerbot/voice-relay/manifest.json', 'utf8')) as { actions: Array<{ group: string }> }; expect(new Set(manifest.actions.map((action) => action.group))).toEqual(new Set(['THSV Addon - Voice Relay'])); const source = await readFile('packages/streamerbot/voice-relay/src/Speak.cs', 'utf8'); expect(source).toContain('CPH.TtsSpeak'); expect(source).not.toMatch(/Log(?:Info|Warn|Error)\([^\n]*message/iu); }); });
