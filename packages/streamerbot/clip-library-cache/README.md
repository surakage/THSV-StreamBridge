# THSV StreamBridge — Clip Library Cache

Import the `.sb`, leave **Refresh** triggerless, and approve only its stable action in the add-on wizard card. The action uses Streamer.bot's existing Twitch connection and returns bounded metadata; it downloads no videos and exposes no credential. The generated action is excluded from Pending and Action History because it is internal maintenance.
