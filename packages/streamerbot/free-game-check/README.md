# THSV StreamBridge - Free Game Check

Import the generated `.sb`, keep **Refresh** triggerless, and approve only its stable action ID in the wizard. The action calls only GamerPower's public HTTPS API, bounds its response, and relays normalized metadata through the existing Streamer.bot WebSocket.
