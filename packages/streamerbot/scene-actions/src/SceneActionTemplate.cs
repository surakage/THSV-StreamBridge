// Purpose: Safe starter target for THSV Scene Actions. Add your own sub-actions below or replace
// this Execute C# Code block. Renaming the action is safe because StreamBridge maps its stable ID.
// References: mscorlib.dll and System.dll.
using System;

public class CPHInline
{
    public bool Execute()
    {
        // Available arguments include sceneProvider, sceneName, sceneOldName,
        // sceneConnectionId, sceneConnectionName, and sceneActionMappingId.
        return true;
    }
}
