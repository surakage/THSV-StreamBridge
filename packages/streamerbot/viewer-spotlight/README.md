# THSV StreamBridge — Viewer Spotlight

Import the `.sb` and keep both actions triggerless. Approve **Settle Reward** only when Twitch reward requests are enabled. Approve **Discord Snapshot** only when Discord snapshots are enabled, and replace its private webhook Set Argument in Streamer.bot.

Rejected, expired, or undisplayable reward requests are refunded when that option is enabled. Other reward IDs are ignored.
